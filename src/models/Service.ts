export type Service = {
  id: string;
  name: string;
  description: string;
  base_price: number;
  provider_id: string;
  category_id: string;
  created: string;
  updated: string;
  photos: string[];
};
